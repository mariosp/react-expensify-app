import React from 'react';
import {Link} from 'react-router-dom';

const NotFoundPage = ()=>(
    <div>
    404
    THis if from my not found comp <Link to='/'> Dashboard </Link>
    </div>
);

export default NotFoundPage;
